# Marks this directory as its own pnpm workspace root, independent of the
# repository root's workspace (see ../../pnpm-workspace.yaml). Without this,
# `pnpm install` here would walk up and share the root's lockfile — this
# example manages its own pnpm-lock.yaml instead.
packages: []
